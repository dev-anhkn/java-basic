package org.example.asynchronous.run;import java.util.concurrent.CompletableFuture;public class BookingService  {    public boolean bookTicket(String customerName, String movie, int seatNumber) {        // Xử lý đặt vé (giả lập bằng in ra màn hình)        System.out.println("[BookingService] Vé đã được đặt cho "                + customerName + " xem " + movie + " tại ghế " + seatNumber);        // Ghi log vào file nhưng chạy trong luồng khác        CompletableFuture.runAsync(new LogTask(customerName, movie, seatNumber));        return true;    }}