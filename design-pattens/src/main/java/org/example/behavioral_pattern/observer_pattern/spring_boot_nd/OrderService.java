package org.example.behavioral_pattern.observer_pattern.spring_boot_nd;import org.example.behavioral_pattern.observer_pattern.spring_boot.Order;import org.springframework.stereotype.Service;import java.util.ArrayList;import java.util.List;import java.util.UUID;@Servicepublic class OrderService {    private final List<Observer> observers = new ArrayList<>();    public void registerObserver(Observer observer) {        observers.add(observer);    }    public void createOrder(String email, double amount) {        Order order = new Order(UUID.randomUUID().toString(), email, amount);        System.out.println("✅ Đơn hàng đã tạo: " + order.getId());        // Notify all observers        for (Observer observer : observers) {            observer.update(order);        }    }}