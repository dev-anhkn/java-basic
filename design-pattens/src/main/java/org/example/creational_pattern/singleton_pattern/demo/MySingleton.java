package org.example.creational_pattern.singleton_pattern.demo;import org.slf4j.Logger;import org.slf4j.LoggerFactory;public class MySingleton {    private static final Logger logger = LoggerFactory.getLogger(MySingleton.class);    private static MySingleton instance;    // Constructor private để ngăn tạo instance bên ngoài    private MySingleton() {        logger.info("Creating MySingleton instance!");    }    // Hàm static để truy cập instance duy nhất (lazy initialization)    public static MySingleton getInstance() {        if (instance == null) {            instance = new MySingleton();        }        return instance;    }    public void showMessage(String msg) {        logger.info("Message: {} | Instance: {}", msg, this.hashCode());    }}