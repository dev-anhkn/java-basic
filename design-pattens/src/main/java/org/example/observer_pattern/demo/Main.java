package org.example.observer_pattern.demo;public class Main {    public static void main(String[] args) {        FacebookNewsFeed newsFeed = new FacebookNewsFeed();        User alice = new User("Alice");        User bob = new User("Bob");        User charlie = new User("Charlie");        // Alice và Bob theo dõi        newsFeed.attach(alice);        newsFeed.attach(bob);        // Đăng tin        newsFeed.postStatus("Alice vừa đăng ảnh mới!");        // Charlie follow sau        newsFeed.attach(charlie);        newsFeed.postStatus("Bob vừa live stream!");        // Bob hủy theo dõi        newsFeed.detach(bob);        newsFeed.postStatus("Charlie vừa đổi ảnh bìa.");    }}