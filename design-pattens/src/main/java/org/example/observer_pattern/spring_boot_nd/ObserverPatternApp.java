package org.example.observer_pattern.spring_boot_nd;import org.springframework.boot.CommandLineRunner;import org.springframework.boot.SpringApplication;import org.springframework.boot.autoconfigure.SpringBootApplication;@SpringBootApplicationpublic class ObserverPatternApp implements CommandLineRunner {    private final OrderService orderService;    private final OrderEmailNotifier emailNotifier;    private final OrderLoggingService loggingService;    private final AdminDashboardNotifier dashboardNotifier;    public ObserverPatternApp(            OrderService orderService,            OrderEmailNotifier emailNotifier,            OrderLoggingService loggingService,            AdminDashboardNotifier dashboardNotifier    ) {        this.orderService = orderService;        this.emailNotifier = emailNotifier;        this.loggingService = loggingService;        this.dashboardNotifier = dashboardNotifier;    }    public static void main(String[] args) {        SpringApplication.run(ObserverPatternApp.class, args);    }    @Override    public void run(String... args) {        // Đăng ký observers        orderService.registerObserver(emailNotifier);        orderService.registerObserver(loggingService);        orderService.registerObserver(dashboardNotifier);        // Tạo đơn hàng        orderService.createOrder("customer@example.com", 299.99);    }}