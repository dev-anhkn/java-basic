package org.example.singleton_pattern;public class MySingleton {    private static MySingleton instance;    // Constructor private để ngăn tạo instance bên ngoài    private MySingleton() {        System.out.println("Creating MySingleton instance!");    }    // Hàm static để truy cập instance duy nhất (lazy initialization)    public static MySingleton getInstance() {        if (instance == null) {            instance = new MySingleton();        }        return instance;    }    public void showMessage(String msg) {        System.out.println("Message: " + msg + " | Instance: " + this.hashCode());    }} class Main {    public static void main(String[] args) {        // Lần đầu lấy instance => tạo mới        MySingleton obj1 = MySingleton.getInstance();        obj1.showMessage("Hello from obj1");        // Lần sau lấy instance => dùng lại instance cũ        MySingleton obj2 = MySingleton.getInstance();        obj2.showMessage("Hello from obj2");        // So sánh 2 biến có cùng instance không        if (obj1 == obj2) {            System.out.println("obj1 và obj2 là cùng một đối tượng Singleton!");        } else {            System.out.println("2 đối tượng khác nhau (KHÔNG đúng với Singleton!)");        }    }}