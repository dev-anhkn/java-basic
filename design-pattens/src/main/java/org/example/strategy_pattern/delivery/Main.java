package org.example.strategy_pattern.delivery;public class Main {    public static void main(String[] args) {        ShippingService service = new ShippingService();        Order lightOrder = new Order(800);   // < 1kg        Order heavyOrder = new Order(1500);  // > 1kg        System.out.println("--- Light Order (800g) ---");        for (EShippingType type : EShippingType.values()) {            System.out.println(type + ": " + service.getShippingFee(lightOrder, type) + "k");        }        System.out.println("\n--- Heavy Order (1500g) ---");        for (EShippingType type : EShippingType.values()) {            System.out.println(type + ": " + service.getShippingFee(heavyOrder, type) + "k");        }    }}